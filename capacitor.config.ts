import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.github.godrenech.cddaguide",
  appName: "CDDA物品浏览器离线版by_ab_irdiden",
  webDir: "dist",
};

export default config;
